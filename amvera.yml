meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  skip: false
  additionalCommands: apt-get update && apt-get install -y python3 make g++ && npm rebuild sqlite3 --build-from-source

run:
  command: npm run start
  containerPort: 80
  persistenceMount: /data
