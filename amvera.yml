meta:
  environment: node
  toolchain:
    name: npm
    version: 22

run:
  scriptName: app.js
  persistenceMount: /data
  containerPort: 80

artifacts:
  - /app/frontend
  - /app.js      # Скопировать файл app.js
  - /package.json
  - /database    # Если нужно